<%= form_with model: [:admin, song], local: true do |f| %>
  <div class="form-group row mt-5">
    <%= f.label :name, "曲名", class: "col-4 col-form-label" %>
    <%= f.text_field :name %>
  </div>
  <div class="form-group row mt-5">
    <%= f.label :composer_id, "作曲者", class: "col-4 col-form-label" %>
    <%= f.collection_select(:composer_id, composers, :id, :name, {prompt: '--選択してください--' }, {class: 'form-select'}) %>
  </div>
  <div class="form-group row mt-5">
    <%= f.label :genre_id, "ジャンル", class: "col-4 col-form-label" %>
    <%= f.collection_select(:genre_id, genres, :id, :name, {prompt: '--選択してください--' }, {class: 'form-select'}) %>
  </div>
  <%= f.submit '登録' %>
<% end %>