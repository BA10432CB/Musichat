<%= form_with model: [:admin, genre], local: true do |f| %>
  <div class="form-group col mt-4">
    <%= f.label :name, "ジャンル名"%><br>
    <%= f.text_field :name %>
  </div>
  <div style="text-align: right;">
    <%= f.submit 'ジャンル登録', class: 'btn btn-create' %>
  </div>
<% end %>