<%= form_with model: [:admin, composer], local: true do |f| %>
  <div class="form-group row mt-5">
    <%= f.label :name, "作曲者名", class: "col-4 col-form-label" %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit '作曲者登録' %>
<% end %>